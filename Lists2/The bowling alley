n,k= map(int, input().split())
pins = ["I"] * n
for i in range(k):
    x1,x2= map(int, input().split())
    for j in range(x1-1,x2):
        pins[j]="."
for i in pins:
    print(i,end="")
#print(*pins,end="")
