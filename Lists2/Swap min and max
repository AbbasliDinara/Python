a = input().split()
my_list=[]
my_list.append(a)
first=int(a[0])
max_val=int(a[0])
min_val=int(a[0])
first1=int(a[0])
index_max=0
index_min=0
for i in range(len(a)):
    if int(a[i])>first:
        max_val=int(a[i])
        index_max=i
    first=int(a[i])
    if int(a[i])<first1:
        min_val=int(a[i])
        index_min=i
    first=int(a[i])
a[index_max], a[index_min] = a[index_min], a[index_max]
#print(max_val)
print(*a)
