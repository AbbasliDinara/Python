a, b = map(int, input().split())

array=[]
for i in range(a):
        row = list(map(int, input().split()))
        array.append(row)

n,m = map(int, input().split())
for row in array:
    row[n],row[m]=row[m],row[n]

for row in array:
    print(*row)
