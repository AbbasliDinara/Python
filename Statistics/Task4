#Sual 4: Fərz edək ki, zər 4 üzlüdür VƏ saxta zərdir. 2 düşmə ehtimalı digərlərindən 2 dəfə çoxdur.

d = {1:0, 2:0, 3:0, 4:0}

n = 10000


for i in range(n):
    zer1 = np.random.randint(1,5)
    if zer1==2:
        d[zer1] += 2
    else:
        d[zer1] += 1

for zer1 in d:
    d[zer1]=d[zer1]/n
print(d)
